<div id="{{ $id }}" class="card-body d-flex flex-column px-0 pb-0">
	<div class="mb-4 px-9">
		<div class="d-flex align-items-center mb-2">
			<span class="{{ $numClass }}">{{ $number }}</span>
		</div>
		<span class="fs-6 fw-semibold text-gray-400">{{ $name }}</span>
	</div>
	<div>
		{{ $body }}
	</div>
</div>